[pytest]
pythonpath = ../src
addopts = -ra
markers =
    unit: fast isolated unit tests
    integration: file-level integration tests
    e2e: full end-to-end tests
    slow: slow tests
    security: security-focused tests
    perf: performance-oriented tests
    ocr: tests requiring OCR behavior
